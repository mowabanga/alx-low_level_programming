#include <string.h>
#include "main.h"

/**
 * main - length of the string
 *_strlen - measures the length of a string
 * return - length
 *@s - variable input
 */

int _strlen(char *s)
{
	return (strlen(s));
}
